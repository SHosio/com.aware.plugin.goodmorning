<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aware.plugin.goodmorning">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@drawable/ic_launcher">

        <service android:name="com.aware.plugin.goodmorning.Plugin" android:exported="true" />
        <activity android:name="com.aware.plugin.goodmorning.Settings" android:exported="true" />
        <receiver android:name="com.aware.plugin.goodmorning.AlarmReceiver" />

    </application>

</manifest>
